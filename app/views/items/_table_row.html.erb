<tr>
    <td><%= item.name %></td>
    <td><%= item.price %></td>
    <td><%= link_to '<i class="fas fa-eye"></i> Show'.html_safe, item %></td>
    <td><%= link_to '<i class="fas fa-edit"></i> Edit'.html_safe, edit_item_path(item) %></td>
    <td><%= link_to '<i class="fas fa-trash"></i> Destroy'.html_safe, item, method: :delete, data: { confirm: 'Are you sure?' } %></td>

  </tr>